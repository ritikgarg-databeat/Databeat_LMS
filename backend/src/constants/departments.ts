/**
 * Departments are user-created data (see the departments module), not a fixed constant list.
 * Reserved for department-related constant values that are NOT data — e.g. name length caps.
 */
export const MAX_DEPARTMENT_NAME_LENGTH = 80;
