/**
 * Departments are user-created data (see the Departments feature), not a fixed constant list.
 * This file is reserved for department-related constant values that are NOT data — e.g. a
 * max-departments-per-org cap, if one is ever introduced.
 */
export const MAX_DEPARTMENT_NAME_LENGTH = 80;
