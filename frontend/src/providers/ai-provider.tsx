/**
 * Placeholder for the future AI Provider (AI tutor chat session state, streaming
 * connection — see ARCHITECTURE.md §14). Pass-through today; implemented as part
 * of the `ai` feature.
 */
export function AIProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
