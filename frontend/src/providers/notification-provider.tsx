/**
 * Placeholder for the future Notification Provider (live in-app notification feed,
 * unread count, push-channel subscription — see ARCHITECTURE.md §15). Pass-through today;
 * implemented as part of the `notifications` feature.
 */
export function NotificationProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
