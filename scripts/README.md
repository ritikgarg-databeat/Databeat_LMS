# scripts/

Reserved for repo-level utility scripts that operate across both `frontend/` and `backend/`
(e.g. a release-tagging script, a combined health-check script, a storage-migration helper
when moving from local disk to S3/R2 — see ARCHITECTURE.md §22). Empty today — this is
project foundation only, per the current phase's scope.
